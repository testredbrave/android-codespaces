# Android Development in GitHub Codespaces

This repository contains configuration for running Android emulator in GitHub Codespaces.

## Features
- Pre-configured Android SDK
- Android Emulator (API 33)
- ADB support

## Usage
1. Create codespace from this repository
2. Wait for the setup to complete (takes about 10-15 minutes)
3. Use `adb` commands to interact with the emulator